<%= render 'welcome' %>

<%= "Welcome back #{@user.name}" %>

<h3>My Lists</h3>

  <% @lists.each do |list|%>
    <%= list.title %>
    <ol><% list.products.each do |product| %>
        <li><%= link_to product.name, product %></li>
    </ol><% end %>
  <% end %>

<%= @no_lists %>
